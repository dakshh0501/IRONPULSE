# FIREBASE → SUPABASE IMPORT PLAN (Step 5B — Dry-Run / Step 6 — Execution / Step 7B — Executed)

Status: **Step 7B EXECUTION COMPLETE (2026-08-14) — 169 rows imported.** See `docs/SUPABASE_DATA_IMPORT_REPORT.md`. Decisions approved, gates GREEN; import pipeline `scripts/migration/import_to_supabase.mjs` executed all stages with post-import validation PASS. No Firebase writes performed.
Generated by `scripts/migration/dry_run_import.js` against `ironpulse-32f31` (2026-08-14).
Rules source of truth: [`FIREBASE_IMPORT_RULES.md`](./FIREBASE_IMPORT_RULES.md).
Schema source of truth: `supabase/migrations/0001_initial_schema.sql`.

---

## 1. Result Summary

| Bucket | Rows | Meaning |
|---|---|---|
| **SAFE_IMPORT** | 152 | Importable as-is after documented coercions (see §7) |
| **MANUAL_REVIEW** | 45 | Importable only after a decision (§5, §6) |
| **QUARANTINE** | 61 | Not importable without remediation; excluded by default |
| **Total** | 258 | = 5A inventory total (0 read errors, 0 drift) |

Per-collection (from `migration-output/summaries/summary.json`):

| Collection | Source | Safe | Manual | Quarantine |
|---|---|---|---|---|
| users → profiles | 35 | 8 | 27 | 0 |
| gyms | 16 | 7 | 6 | 3 |
| subscriptions | 7 | 7 | 0 | 0 |
| subscriptionHistory | 2 | 2 | 0 | 0 |
| members | 1 | 0 | 0 | 1 |
| plans | 28 | 18 | 6 | 4 |
| attendance | 49 | 0 | 0 | 49 |
| notifications | 18 | 14 | 0 | 4 |
| settings | 7 | 1 | 6 | 0 |
| licenseHistory | 11 | 11 | 0 | 0 |
| referralCodes | 1 | 1 | 0 | 0 |
| aiConversations | 15 | 15 | 0 | 0 |
| aiConversationMessages | 65 | 65 | 0 | 0 |
| contactMessages | 3 | 3 | 0 | 0 |
| 18 empty collections | 0 | 0 | 0 | 0 |

---

## 2. Execution Contract

1. **Dry-run only**: the transformer reads Firebase via REST (Firebase CLI refresh token) and writes JSON manifests under `migration-output/`. It never connects to Supabase and never writes to Firebase.
2. **Guards**: the script aborts if `FIREBASE_EMULATOR`, `SUPABASE_SERVICE_ROLE_KEY`, or `FIREBASE_SERVICE_ACCOUNT` are set in the environment.
3. **Sanitization is a hard requirement**: all artifacts are masked/stripped (GATE A); real PII/credentials are re-read from Firebase at actual import time, never carried in files.
4. The real import (Step 7B) ran `scripts/migration/import_to_supabase.mjs` (staged PostgREST service_role inserts, idempotent, per-stage atomic, stops on FK/uniqueness violations) — **COMPLETE, 169 rows**, post-import verification PASS (`scripts/migration/post_import_verify.mjs`). Credentials supplied per-run via a temp file outside the repo; deleted after execution.

---

## 3. Import Order (12 stages, FK-safe)

| Stage | Table(s) | FK rationale |
|---|---|---|
| 1 | `gyms` | All other rows reference gyms; `gyms.id` = preserved doc id |
| 2 | `profiles` | Auth provision (see §4); `profiles.gym_id → gyms.id`; `profiles.firebase_uid` unique |
| 3 | `plans`, `settings`, `referral_codes`, `contact_messages` | Reference gyms only |
| 4 | `trainers` | gym_id + firebase_uid (source empty — no-op) |
| 5 | `members` | gym_id + auth_uid; parent of attendance/plans history |
| 6 | `subscriptions` → `subscription_history` → `payment_attempts` → `payments` | subscription → gym; history/attempts → subscription (sources: 7/2/0/0) |
| 7 | `attendance`, `progress_logs`, `diet_plans`, `workout_plans` | Member-referenced (attendance 49 quarantined, others empty) |
| 8 | `notifications` | user_id → profiles.firebase_uid, gym_id nullable |
| 9 | `referrals` → `reward_ledger` → `discount_coupons` → `referral_audit_logs` | Referrer/referred → profiles (all empty in source) |
| 10 | `support_tickets` (+replies/notes/attachments), `feature_requests`, `audit_log`, `generated_reports` | Self-contained (all empty in source) |
| 11 | `ai_conversations` → `ai_conversation_messages` | Conversation first (65 messages reference 15 conversations, all parent rows present) |
| 12 | `whatsapp_campaigns` → `whatsapp_logs`, `licensed_devices`, `license_history`, `plan_templates` | Self-contained (license_history 11 safe; others empty) |

Empty collections are imported as empty tables; they are listed so the migration is provably complete.

---

## 4. Auth Provisioning (Stage 2)

- `profiles.id` = deterministic UUID (uuid5-style) of the Firebase uid; `profiles.firebase_uid` = the uid itself.
- **8 SAFE_AUTH_PROVISION**: 7 `gym_owner` (gymId direct or backfilled from `gyms.ownerUid` — R-OWNER-BACKFILL) + 1 `super_admin` (`gym_id` NULL — cosmetic COMMANDOGYM ref dropped).
- **27 MANUAL_REVIEW** (decisions in §6 D1): 8 rejected (→ `account_disabled=true`), 6 `gym_owner_pending`, 12 member/trainer without a resolvable gym, 1 owner (`bRKcg3fw`) whose gym was deleted.
- **0 BLOCKING** — every account has a valid email + role.
- No Auth user is created by the dry-run; the manifest prepares the `firebase_uid → id` mapping only.

---

## 5. Gates (current status)

| Gate | Status | Detail |
|---|---|---|
| A — no sensitive-value leakage | **GREEN** | deny-list scan of every artifact: 0 files, 0 unmasked emails/phones |
| B — canonical IDs resolvable (safe+manual rows) | **RED (1)** | `users/bRKcg3fw...` → gym `RAlIvHAT...` deleted; decision D1.4 |
| C — all required gym relationships valid | **RED (9)** | 6 rejected + 3 orphan gyms — decision D2 |
| D — plan vocabulary approved | **RED (4)** | `Basic plan`, `kickstart`, `legacy`, `Titan` — decision D3.2 |
| E — attendance orphan policy | **REVIEW REQUIRED** | 49/49 quarantined — decision D4 |
| F — deterministic identifiers collision-free | **GREEN** | referral codes, payment ids, invoice nos, license keys, uids, gym ids: 0 duplicates |
| G — import row counts reconciled | **GREEN** | every collection: source == safe + manual + quarantine |

Expected post-decision state: A/B/F/G GREEN; C, D, E resolve to GREEN or to documented exclusions.

---

## 6. Decisions Required (before Step 6)

> **Status: APPROVED — 2026-08-14.** All seven decisions were signed by the developer ("I approve all 7 migration decisions exactly as documented"). The full approval matrix (issue / evidence / counts / consequence / reversibility / recommendation per block, plus special cases A–D) is in **[`FIREBASE_IMPORT_DECISIONS.md`](./FIREBASE_IMPORT_DECISIONS.md)**, and the summary in **[`FIREBASE_IMPORT_APPROVAL_SUMMARY.md`](./FIREBASE_IMPORT_APPROVAL_SUMMARY.md)**. Import is **NOT EXECUTED** — execution requires an explicit request.

### D1 — User rows (27 MANUAL_REVIEW)
1. **8 rejected accounts** — import with `account_disabled=true` (recommended: YES — preserves audit history; `profile.id` mapping stays stable) or exclude entirely?
2. **6 `gym_owner_pending`** — import with role `gym_owner_pending` + `gym_id NULL` (recommended: YES — no gym doc claims any of them; verified: all 16 gyms are owned by other users or orphans) or exclude?
3. **12 member/trainer rows with no gym** (`default`/absent gymId) — import with `gym_id NULL` (recommended: YES — keeps accounts; gym link re-attached later) or exclude?
4. **`bRKcg3fw...` (owner of deleted gym `RAlIvHAT...`)** — import with `gym_id NULL` (recommended: YES; no gym claims this ownerUid, relationship is not preservable) or exclude? (This is GATE B's 1 unresolved ref.)

### D2 — Gym rows (6 MANUAL + 3 QUARANTINE)
1. 6 **rejected gyms** — import with `approval_status='rejected'` (recommended: YES — FK-valid, tied to D1.1) or exclude?
2. 3 **orphan gyms** (`3u8ObSiW`, `6XND7bUX`, `Vh305r5D` — no owner user) — recommended: **PRESERVE + QUARANTINE** (no owner invented; `Vh305r5D` is approved-status and flagged for developer confirmation, but the safe default stands).

### D3 — Plan rows (6 MANUAL + 4 QUARANTINE)
1. 6 plans with `gymId='default'` sentinel — recommended: **EXCLUDE** (no tenant; canonical names already exist under the 3 real gyms).
2. 4 plans with no consumer-evidence names (`Basic plan`, `kickstart`, `legacy`, `Titan`) and missing gymId — recommended: **EXCLUDE** (no semantic mapping — duration coincidence is not evidence; see Special Case C in the decisions doc).

### D4 — Attendance (49 QUARANTINE)
All lack gymId; 30 also lack date/time; 9 distinct memberIds resolve to nothing. Recommended policy: **PRESERVE + QUARANTINE with manifest** — legacy/unattached test data; historical attendance is non-authoritative for the new tenant. Approve this policy to flip GATE E.

### D5 — Settings (1 SAFE + 6 MANUAL_REVIEW)
`platform` is now **SAFE** (`gym_id='platform'`, guard-exempt, consumed by `PlatformSettings.jsx`). The remaining 6 (`default:gym`, `gym`, `notifications`, `theme`, `pricing`, `profile_…`) fail `guard_settings_gym`. Recommended: **EXCLUDE all 6** (no tenant named; `pricing` has no consumer evidence; `profile_…` has no schema slot).

### D6 — Member `CX5jO1U3pxm5KgzWtV2E`
Contains a sensitive field (stripped — never migrated), no gymId, no authUid. Recommended: **EXCLUDE** (quarantine-preserve); re-create the member cleanly in the app if real.

### D7 — Notifications (4 QUARANTINE)
`userId` missing or unresolved (user_id NOT NULL). Recommended: **EXCLUDE the 4** (quarantine-preserve); the 14 safe rows import with `gym_id NULL` (R-SENTINEL-NULL).

---

## 6A. Pre-Import Approval Gates

Current state (2026-08-14, post-approval): **ALL GATES GREEN** — A GREEN · B GREEN · C GREEN · D GREEN · E GREEN · F GREEN · G GREEN. Decisions 1–7 signed; no gate is marked green falsely (each flip is backed by a signed decision or a verified check).

### Must be GREEN before any `--apply` import starts
| Gate | Status (post-approval) | Evidence required |
|---|---|---|
| **A** — no sensitive-value leakage | **GREEN** | deny-list scan of all artifacts (0 hits) |
| **B** — canonical IDs resolvable | **GREEN** | D1.4 approved — `bRKcg3fw` imports with `gym_id NULL` (deleted-gym ref documented, not resolved) |
| **E** — attendance orphan policy | **GREEN** | D4 policy approved — all 49 remain quarantined by decision |
| **F** — deterministic identifiers collision-free | **GREEN** | collision check (0) |
| **G** — import row counts reconciled | **GREEN** | per-collection reconciliation (0) |

### Gates resolved by the decisions themselves
| Gate | Mechanism |
|---|---|
| **C** — required gym relationships valid | **GREEN** — D1.1 + D2.1 approved: the 6 rejected gyms import with resolvable owners (FK-valid); the 3 orphan gyms remain quarantined (allowed). Zero *importable* gyms with unresolved owners |
| **D** — plan vocabulary approved | **GREEN** — D3.2 approved: the 4 unknown-name plans are formally EXCLUDED (decision), not mapped |

### May remain QUARANTINED without blocking the initial import
The 61 quarantine rows, once the exclusions above are signed: 49 attendance + 3 orphan gyms + 4 unknown-name plans + 1 member (`CX5jO1U3...`) + 4 notifications. No action required; manifests retained for reopen.

### May remain MANUAL_REVIEW without blocking the initial import
All 45 manual rows become importable after the §6 approvals (8 rejected users, 6 pending owners, 12 gymless profiles, 1 deleted-gym owner, 6 rejected gyms, 6 sentinel plans, 6 tenantless settings — the exclusions simply route them to quarantine). **Nothing in MANUAL_REVIEW blocks the initial import** once its decision row is signed.

### Evidence required to REOPEN a blocked decision
| Blocked item | Reopen trigger |
|---|---|
| 3 orphan gyms (D2.2) | A users doc appears whose `ownerUid` matches (`3u8ObSiW`/`6XND7bUX`/`Vh305r5D`), or developer-provided owner mapping |
| 4 unknown-name plans (D3.2) | New consumer evidence (source grep hit) or explicit developer mapping naming a gym + canonical name |
| 6 sentinel plans (D3.1) | Developer names a real gym as tenant for the `default` catalog |
| 6 tenantless settings (D5) | Developer names the gym (and doc) mapping |
| 49 attendance rows (D4) | A member doc appears matching one of the 9 dangling `memberId`s (id or authUid), or a developer mapping |
| `bRKcg3fw` gym (D1.4) | Gym doc `RAlIvHAT...` recreated, or developer decision to map elsewhere |
| 4 notifications (D7) | A users doc appears matching the missing `userId`s |

No gate is marked GREEN falsely: B/C/D/E flip only on signed decisions; A/F/G are verified GREEN by the transformer run.

---

## 7. Transformation Summary (evidence-backed)

- **Deterministic UUID**: sha256(`IRONPULSE:{legacyId}`) → uuid5-style; identical across runs (GATE F).
- **`gyms.id`**: preserved doc id (text PK).
- **Plans**: `duration` coerced string → integer minutes (`7 Days`→10080, `1 Month`→43200, `3 Month`→129600, `12 Months`→525600, `6 Month`→259200, `1 year`→525600, `1 Day`→1440, `1 Month`×n); `duration_days` = `durationDays` (int, authoritative). `name` stays verbatim (vocabulary gate D).
- **Users**: `gym_id` from `gymId` when resolvable, else backfilled from the single gym claiming `ownerUid`, else NULL + reason. `account_disabled` for rejected. Emails/phones masked in artifacts.
- **Notifications**: sentinel `gymId='default'` → NULL (nullable FK); priority/type/subtype/role preserved (observed: type `gym`, subtype `gym_rejected`×11 / `gym_approved`×7, role `admin`, priority `high`).
- **Subscriptions**: `expiry_date` normalized to `YYYY-MM-DD`; observed values all within enum (`trial`/`active`, `paid`, amount 9999, method `Not Set` — kept verbatim); `license_key` NULL (absent in source).
- **Settings**: composite `(gym_id, doc_id)` from doc id (`default:gym` → `default`/`gym`); `data` jsonb = scrubbed remaining fields.
- **Members**: sensitive fields stripped; `weight` coerced to numeric (string `"64"` → 64) or flagged.
- **Empty collections**: zero-row import, documented.

---

## 8. Sanitization Summary

- Deny-list key stripping: password/passwd/salt/hash/secret/token/credential/api-key/private-key variants (recursive, incl. nested `settings.data`, `gym.subscription`, `changes`, `metadata`).
- Email masking (`j***@domain`) and phone masking (`91***789`) in every artifact.
- GATE A re-scans all artifacts after generation (0 hits).

---

## 9. Artifacts

```
migration-output/
  safe/            profiles.json gyms.json subscriptions.json subscription_history.json plans.json
                   notifications.json license_history.json referral_codes.json ai_conversations.json
                   ai_conversation_messages.json contact_messages.json
  manual-review/   profiles.json gyms.json plans.json settings.json
  quarantine/      members.json gyms.json plans.json attendance.json notifications.json
  summaries/       summary.json gates.json user-auth-classification.json
                   attendance-quarantine-manifest.json deterministic-identifiers.json
```

---

## 10. Readiness Statement

- Dry-run transformer: **ready** (deterministic, idempotent, sanitized, gated).
- 152 rows are importable as-is; 106 rows are governed by the decisions in §6/§6A (45 manual + 61 quarantine; exclusions route manual rows to quarantine).
- Build: `npm run build` passes (0 errors / 0 warnings) — verified after every change.
- Next step (Step 6): decisions are **signed (2026-08-14)** and all gates are GREEN; run the transformer with `--apply` against Supabase in the staged order — **only on explicit developer request. Import NOT EXECUTED.**

## 11. Step 6 Execution Status (actual import — 2026-08-14)

| Stage | Table(s) | Status |
|---|---|---|
| 1 | gyms | **NOT STARTED** — blocked (owner_uid FK → profiles) |
| 2 | profiles | **NOT STARTED** — blocked (profiles.id FK → auth.users; no auth provisioning authorized) |
| 3–16 | all remaining stages | **NOT STARTED** — cascade from profiles |

- Actual import execution: **STOPPED AT PRE-FLIGHT** — detailed in [`SUPABASE_DATA_IMPORT_REPORT.md`](./SUPABASE_DATA_IMPORT_REPORT.md).
- Blocker A: no write-capable Supabase credential in this environment (no access token, no DB password; only public anon key which cannot write under RLS).
- Blocker B: `profiles.id` references `auth.users(id)` (0001:92) and every user-scoped FK references `profiles(firebase_uid)` — zero stages importable without Supabase Auth provisioning (forbidden by Step 6 constraints) or a schema change (forbidden).
- Reconciliation status: dry-run reconciliation stands (258 = 152 + 45 + 61, GATE G GREEN). Live-row reconciliation: **N/A — no rows written**.
- **NOT marked complete**: Auth, Storage, Realtime, Functions, application cutover.

### No-writes confirmation
`dry_run_import.js` contains no Supabase import, no `postgrest`/`pg` client, and no Firestore write; it aborts under emulator/service-role env. **NO DATA WAS WRITTEN TO FIREBASE OR SUPABASE.**

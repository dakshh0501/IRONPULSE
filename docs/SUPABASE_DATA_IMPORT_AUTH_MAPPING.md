# SUPABASE DATA IMPORT — AUTH ID MAPPING (STEP 7A, READ-ONLY)

**Date:** 2026-08-14 · **Mode:** validate-live-auth · **Validation:** PASS

Generated by `scripts/migration/auth_id_mapper.js` (READ-ONLY — no Auth writes, no Firebase access, no INSERT/UPDATE/DELETE). Source of truth for the approved set: `migration-output/summaries/auth-provisioning-plan.json` (approved 2026-08-14).

## 1. Totals

| Item | Count |
|---|---|
| Total Firebase users (classified) | 35 |
| Approved Auth identities | 25 |
| Total Supabase Auth users (seen) | 25 |
| Successful mappings | 25 |
| Missing mappings | 0 |
| Duplicate mappings | 0 |
| Unexpected Auth users (foreign firebase_uid) | 0 |
| Rejected-user mappings | 3 |
| — of which banned (banned_until future) | 3 |
| — not banned (STOP) | 0 |
| Legacy-only exclusions (no auth) | 10 |
| Legacy-only users wrongly provisioned (STOP) | 0 |
| Auth users without firebase_uid metadata | 0 |
| Unresolved references (app data) | 0 |

**Verdict: PASS**

## 2. ID Mapping Model (final, STEP 1/3)

| Firebase identity | Supabase key | Note |
|---|---|---|
| profile (user) | `profiles.id` = **`auth.users.id`** (live UUID) | obtained from live Auth via `user_metadata.firebase_uid`; never derived/invented |
| profile (user) | `profiles.firebase_uid` = original Firebase UID | unique, NOT NULL |
| member doc | `members.id` = deterministic UUID (`detUuid(memberDocId)`) | member registry id — NOT auth.users.id |
| trainer doc | `trainers.id` = deterministic UUID (`detUuid(trainerDocId)`) | trainer registry id — NOT auth.users.id |
| gym doc | `gyms.id` = preserved Firestore doc id (text) | tenancy root |
| user refs (text columns) | `* → profiles(firebase_uid)` | DDL-approved TEXT FKs — Firebase UIDs preserved, identity-validated |

## 3. FK Translation Readiness (STEP 4)

**UUID FK columns → members.id / trainers.id** (translated via `detUuid` of the resolved legacy doc id — never raw doc ids, never Firebase UID strings):

| Table | Source field | Target | Resolver |
|---|---|---|---|
| attendance | `memberId` (may be member authUid) | `member_id uuid` | member doc id → `detUuid` |
| attendance | `trainerId` | `trainer_id uuid` | trainer doc id → `detUuid` |
| payments | `memberId` | `member_id uuid` | member doc id → `detUuid` |
| progress_logs | `memberId` | `member_id uuid` | member doc id → `detUuid` |
| progress_logs | `trainerId` | `trainer_id uuid` | trainer doc id → `detUuid` |
| diet_plans / workout_plans | `memberId` | `member_id uuid` | member doc id → `detUuid` |
| whatsapp_logs | `memberId` | `member_id uuid` | member doc id → `detUuid` |
| members | `trainerId` | `trainer_id uuid` | trainer doc id → `detUuid` |

**TEXT FK columns → profiles(firebase_uid) / members(auth_uid) / trainers(auth_uid)** (identity-validated against the approved set; values preserved as Firebase UIDs per approved DDL):

| Table | Columns |
|---|---|
| gyms | `owner_uid` |
| trainers | `auth_uid`, `created_by` |
| members | `auth_uid`, `trainer_auth_uid`, `created_by` |
| subscriptions / subscription_history | `created_by` / `actor_uid` |
| payment_attempts | `auth_uid` |
| diet_plans / workout_plans | `auth_uid`, `assigned_trainer_auth_uid` / `trainer_auth_uid`, `created_by` |
| progress_logs / payments | `auth_uid`, `created_by` |
| attendance | `auth_uid`, `trainer_auth_uid` |
| notifications | `user_id` |
| support_tickets / feature_requests / whatsapp_campaigns / licensed_devices | `created_by` |
| license_history / referral_audit_logs / audit_log | `performed_by` / `performed_by`,`target_uid` / `changed_by` |
| referral_codes / referrals / reward_ledger / discount_coupons | `referrer_uid` / `referred_uid`,`referrer_uid` / `referrer_uid`,`referred_uid`,`user_id` / `user_id` |
| ai_conversations | `user_id` |

**Never left as Firebase UID strings in UUID columns.** Any ref that resolves to a non-approved identity is quarantined as unresolved (never silently imported).

## 4. Credential Stripping (STEP 3)

Stripped from every record before leaving the pipeline (deny-key, recursive): `password`, `passwordHash`, `password_hash`, `passwd`, `salt`, `passwordSalt`, `credentials`, `tokens`/`token`, `apiKey`/`api_key`, `secret`, `clientSecret`, `accessToken`, `refreshToken`, `privateKey`. No password material is ever read, exported, transformed, or reused.

## 5. STOP Conditions (STEP 2)

Exit code 1 (import must not proceed) if ANY of: missing mapping; duplicate mapping (one firebase_uid → multiple auth users); unexpected Auth user carrying a firebase_uid outside the approved set; legacy-only user wrongly provisioned; existing rejected user not banned. The id-map manifest + this document are still written so the state is reviewable.

## 6. Evidence

- Approved plan: `migration-output/summaries/auth-provisioning-plan.json` (35 classified; approved 25 = 22 create + 2 create-banned + G1 canonical).
- G1 canonical (developer-selected, approved): `A0kiygGn` (masked).
- Provisioner manifest: `migration-output/summaries/auth-provisioning-manifest.json` (run `AP-20260814093703` — recorded target UUIDs are the offline evidence used by `--build-map`).
- Reconciliation: `docs/SUPABASE_AUTH_FINAL_RECONCILIATION.md` (3 known remote users: 2 normal + 1 banned; 22 pending provision).

## 7. Next Step

Run `--validate-live-auth` with the runtime service_role credential once provisioning is complete (expected 25/25 PASS, 0 missing/duplicates/unexpected, 3 rejected banned). Only then may the profile/application-data import stage run.